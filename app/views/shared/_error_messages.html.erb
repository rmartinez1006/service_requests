  <% if target.errors.any? %>
   <div id="errorExplanation">
     <h1><%= pluralize(target.errors.count, "") %> Error(es). No se guardó el presupuesto:</h1>
     <ul>
     <% target.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
     </ul>
   </div>
  <% end %>
