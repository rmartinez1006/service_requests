<%= form_for(@catalogs_unit) do |f| %>
  <% if @catalogs_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@catalogs_unit.errors.count, "error") %> prohibited this catalogs_unit from being saved:</h2>

      <ul>
      <% @catalogs_unit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table width="990">
    <tr>
      <td width="80" align="right">
        <%= f.label 'Abreviación:' %><br />
      </td>
      <td>
        <%= f.text_field :abbr , :size => 6%>
      </td>
    </tr>
    <tr>
      <td width="80" align="right">
        <%= f.label 'Nombre:' %><br />
      </td>
      <td>
        <%= f.text_field :name %>
      </td>
    </tr>
  </table>

  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
